[pytest]
testpaths = tests

python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    unit: tests rapides sans navigateur ni reseau reel
    e2e: tests end-to-end via Selenium

addopts = -v --strict-markers
